import { View, Text } from 'react-native';
import React from 'react';
import RootLayout from '../../rootLayout';

const Dashbaord = () => {
  return (
    <RootLayout>
      <View>
        <Text className={`text-white`}>Dashbaord</Text>
      </View>
    </RootLayout>
  )
}

export default Dashbaord;
